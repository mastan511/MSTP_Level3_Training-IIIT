marks = 100
def predict(x):
    return (x*2)+1


def properName(f,l):
    a = f.strip().capitalize()
    b = l.strip().capitalize()
    return a+" "+b

def proper(name):
    return name.title().strip()